# VoiSpeech Coach 최종 검토 범위

이 번들은 기존 React/Vite 프로젝트 전체를 포함하며, 활성 수업 화면을 코치의 의사결정 흐름에 맞춰 재구성했습니다. 유형을 선택하면 우선 도구, 대안 도구, 반응별 다음 선택이 한 화면에 표시됩니다.

첨부된 16개 PDF는 중복 파일을 확인해 14개 고유 연구로 묶어 검토했습니다. 연구 결과는 대상·도구·조건·평가 항목 범위 안에서만 요약했습니다. SLS의 음절별 우선순위와 여섯 유형은 논문이 직접 검증한 고정 진단표가 아니므로 코칭용 관찰 경향과 적용 가설로 표시했습니다.

`CONTENT_REVIEW.md`에는 기존 소스의 주장 후보와 문장별 검토 상태를 기록했습니다. `src/types.ts`와 `src/lib/terms.ts`의 상세 자료도 단정적인 생리 표현을 관찰·비교 중심으로 조정했습니다.

네트워크 의존성 설치가 자동 승인 제한으로 실행되지 않아 이 환경에서는 `npm run build`를 실행하지 못했습니다. 배포 전 저장소에서 `npm install && npm run build`를 실행하면 됩니다.
